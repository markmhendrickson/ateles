# Learn Neotoma Command

## Purpose

Direct users and agents to the canonical learn-neotoma workflow (update MCP instructions from failures) in the learn-neotoma skill.

## Scope

Pointer document only; applies when invoking `/learn_neotoma` (or legacy `/neotoma_learn`) or when improving Neotoma MCP instructions. Full workflow lives in the skill.

---

Canonical workflow now lives in the `learn-neotoma` skill:
`/.cursor/skills/learn-neotoma/SKILL.md`

Invoke with `/learn_neotoma` or `/neotoma_learn` (or equivalent natural-language trigger) to load and execute the skill workflow.
