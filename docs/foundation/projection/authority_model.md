<!-- GENERATED by execution/scripts/render_reading_projection.py — do not edit. -->
<!-- Source: docs/foundation/conformance_suite.md, the conformance matrix. -->

# Reading projection — `authority_model.md`

Every rule `authority_model.md` owns, one entry each: the rule's own statement from `conformance_suite.md`'s matrix, and a link to the heading that argues it. The argument, the cost, the prior art, and the walkthrough are in [`authority_model.md`](../authority_model.md) and are not repeated here (decision 66).

21 rules.

## [The tuple](../authority_model.md#the-tuple)

- **AU-1** — `Indeterminate` is deny; zero grants deny; a raising check denies *[M]*
- **AU-2** — the same: time is read by the checker *[M]*

## [Principals](../authority_model.md#principals)

- **AU-3** — a credential is a binding, never the principal; a magic value compared as `"operator"` *[M]*
- **AU-4** — the same: `operator_profile` carries no authority edge *[M]*
- **AU-5** — the same: a write whose only identity is `user_id` on a shared instance is unattributed *[M]*
- **AU-6** — the same: tenant *[P (`multi_tenant.md` section 7)]*
- **AU-7** — the same: at most one `ownership_grant` per type *[M]*

## [Grants](../authority_model.md#grants)

- **AU-8** — a failed load is a stub nobody starts a runner from; no wildcard from a degraded read *[M]*
- **AU-9** — the same: a denial precedes the effect, is structured, raises `capability_denied`, and is a request never a grant *[M (X-9 closed)]*
- **AU-10** — the same: the denied principal does not route around *[M]*
- **AU-11** — the same: custody by revocability; no credential in the process environment; resolved once per invocation *[M (process inspection is the harness's; the design's rule is about processes it does not own)]*
- **AU-12** — the same: rotation is staged *[M]*
- **AU-13** — the same: the grant is read at every enforcement point; revocation's reach *[M]*
- **AU-21** — (ruling 41): write admission per entity type is default-deny; the grant is the allowlist, read at every enforcement point; a capability naming every type is the fail-open shape; attribution is required besides and prevents nothing *[M]*

## [Attribution](../authority_model.md#attribution)

- **AU-14** — the rule this heading states *[M]*

## [Delegation](../authority_model.md#delegation)

- **AU-15** — attenuation; A-for-B; the chain derived; the hardest chain reconstructible *[M]*

## [Approval](../authority_model.md#approval)

- **AU-16** — yes/no/veto by a required principal; authorized against required approvers; timeout terminal; silence never accepts; no cross-principal auto-approve; notification via roster and channel config *[M]*

## [The raiser of a checkpoint does not resolve it, and the operator's self-resolution is marked](../authority_model.md#the-raiser-of-a-checkpoint-does-not-resolve-it-and-the-operators-self-resolution-is-marked)

- **AU-17** — the raiser does not resolve; the operator's self-resolution is marked *[M (decision 47, ruled 2026-09-06, with 43)]*

## [Structural checks: quorum and separation of duties](../authority_model.md#structural-checks-quorum-and-separation-of-duties)

- **AU-18** — payment's disjoint verifier *[M]*
- **AU-19** — the same: the counting rule; count and disjointness over the checkpoint's principal edges; the thresholds on the `action_policy` per class *[M (48, 49, and 50 ruled 2026-09-06 — `authority_model.md#structural-checks-quorum-and-separation-of-duties`; which checks a class carries is read from the `action_policy` under test, and the class with no value is the row's fail-closed case)]*

## [Initiative, proposal, reprioritization](../authority_model.md#initiative-proposal-reprioritization)

- **AU-20** — initiative approval is the checkpoint; what stops is a task, confirmed by the owner seat through the checkpoint and read back, proposing a grant capability; a budget attenuates, consumption is derived, and what is metered is per class on the policy; credit is a read model *[M (51 to 54 ruled 2026-09-06 — `authority_model.md#initiative-proposal-reprioritization`)]*
