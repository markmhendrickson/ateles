<!-- GENERATED by execution/scripts/render_reading_projection.py — do not edit. -->
<!-- Source: docs/foundation/conformance_suite.md, the conformance matrix. -->

# Reading projection — `vocabulary.md`

Every rule `vocabulary.md` owns, one entry each: the rule's own statement from `conformance_suite.md`'s matrix, and a link to the heading that argues it. The argument, the cost, the prior art, and the walkthrough are in [`vocabulary.md`](../vocabulary.md) and are not repeated here (decision 66).

4 rules.

## Rules

- **VO-1** — retired **type or field names** that must not exist in the record *[M]*
- **VO-2** — retired **status and state values** *[M]*
- **VO-3** — words banned in **prose the swarm emits**: prompts, error messages, checkpoint text, announcements *[M (the checker exists for documents; its extension to emitted text is a contract)]*
- **VO-4** — the **Verbs** table and the **Owner** table *[M (same contract)]*
