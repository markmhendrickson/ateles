<!-- GENERATED by execution/scripts/render_reading_projection.py — do not edit. -->
<!-- Source: docs/foundation/conformance_suite.md, the conformance matrix. -->

# Reading projection — `workflows.md`

Every rule `workflows.md` owns, one entry each: the rule's own statement from `conformance_suite.md`'s matrix, and a link to the heading that argues it. The argument, the cost, the prior art, and the walkthrough are in [`workflows.md`](../workflows.md) and are not repeated here (decision 66).

26 rules.

## [How to read a workflow section](../workflows.md#how-to-read-a-workflow-section)

- **WF-1** — a failing verdict does not advance; `on_fail` reopens; the cap escalates *[M]*

## [Roles named in this document](../workflows.md#roles-named-in-this-document)

- **WF-2** — every role resolves against the roster per declaration scope *[M]*

## [intake](../workflows.md#intake)

- **WF-3** — entry once, at creation; five steps; `classify` writes `action_type` from what the task does *[M]*
- **WF-4** — `link` *[M (U-17 closed by ruling 39; WF-25 is the row)]*
- **WF-5** — `dedupe` *[M (U-23 closed)]*
- **WF-6** — `prioritize` from `priority_rubric`, never the classifier's sense *[M]*
- **WF-7** — `route`: one successor, none, or operator-only; intake makes no action *[M]*
- **WF-8** — `inherits` fast path *[M]*

## [feature](../workflows.md#feature)

- **WF-9** — entry names a repository; `pm` states scope, evidence, basis; `ux`+`arch` parallel and joined; `impl` closes on a pull request artifact with checks passing; `pr_review`, `qa` judgements; `legal` optional by `applies_when`; `merge` is an action, read back, names the successor *[M for structure; R for judgements]*
- **WF-10** — feature fast paths: taken only where the declaration scope declares no workflow of that type *[M]*

## [bug](../workflows.md#bug)

- **WF-11** — the rule this heading states *[M]*

## [security](../workflows.md#security)

- **WF-12** — `release` always; no exploit detail on public artifacts *[M for the successor; R for detail]*

## [copy](../workflows.md#copy)

- **WF-13** — `copy` before `impl`, against `brand_voice`; `ux` optional *[M]*

## [social content](../workflows.md#social-content)

- **WF-14** — `draft_lint` deterministic; `consent` carries the `publish` checkpoint; `approved` only where the gate would not checkpoint *[M]*

## [release](../workflows.md#release)

- **WF-15** — `criteria` from `release_criteria`, unknown holds; `release` read back at terminal state; `verify_deployed` reads the deployed checkout *[M]*

## [outreach](../workflows.md#outreach)

- **WF-16** — the draft is in the record; `disclosure_lint` before `review`; `review` before `consent`; the checkpoint carries the full draft; `send` read back by message id; a change after consent is a new draft, re-entering at `disclosure_lint`; `follow_up` through the same gate *[M (`disclosure_lint`'s check closed the review-only half U-34 named); R for `review`'s remaining judgement on facts, voice, and scope]*

## [payment](../workflows.md#payment)

- **WF-17** — a task naming payee or amount inline fails `classify`; `prepare` from the profile; `verify` disjoint; `consent` carries payee, amount, reference; `pay` keyed; `reconcile` reads terminal state, writes `transaction`, is the verifier's *[M]*

## [research and analysis](../workflows.md#research-and-analysis)

- **WF-18** — unread sources recorded as unread; `persist` before `deliver`, read back *[M for order; R for content]*

## [meeting processing](../workflows.md#meeting-processing)

- **WF-19** — a recording the operator was not party to fails `classify`; `extract` minimizes; `persist` creates tasks entering their own intake; `deliver` creates outreach tasks and never sends *[M for shape; R for content]*

## [operator-only](../workflows.md#operator-only)

- **WF-20** — only the operator-facing agent claims; `present` through `channel_config`; `await` bounded to `rounds_exhausted`; `record` read back; the redirect is by this batch's close, not a new intake *[M]*

## [session digestion](../workflows.md#session-digestion)

- **WF-21** — unread never empty; unverifiable never confirmed; `file` creates and completes none *[M]*

## [What no workflow in this document does](../workflows.md#what-no-workflow-in-this-document-does)

- **WF-22** — the rule this heading states *[M]*

## [Rendering](../workflows.md#rendering)

- **WF-23** — the step tables are render targets of the `workflow` entities; a table edited by hand fails `--check`; an undeclared workflow's table is hand-authored under the same marker and becomes the check's expected content when the entity is declared *[M (contract; whether `render_workflow_docs.py` exists is `status.md`'s)]*

## [Whether a stage names anything a step does not](../workflows.md#whether-a-stage-names-anything-a-step-does-not)

- **WF-24** — the rule this heading states *[P (open decision 33; finding U-30 points at it, and the matrix observes nothing under either option)]*

## [What `link` attaches, and what it leaves to hydration](../workflows.md#what-link-attaches-and-what-it-leaves-to-hydration)

- **WF-25** — (ruling 39): `link` attaches every record the task names — an external one as an artifact, one in the record by `REFERS_TO` — and nothing on relevance alone; a name resolving to nothing creates nothing; hydration resolves a step's declared reads from those anchors; context a step discovers is written back as the same edge *[M]*

## [planning](../workflows.md#planning)

- **WF-26** — entry by the record's one live instance; `survey` names its reads; `judge` writes only findings; `amend` writes through `amend_<level>` actions, creates tasks under the record and the parent, creates the next instance before its sign-off, and names no successor; optional review steps by `applies_when` carry a role's findings *[M for structure; R for judgements]*
