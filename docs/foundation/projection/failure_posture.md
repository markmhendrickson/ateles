<!-- GENERATED by execution/scripts/render_reading_projection.py — do not edit. -->
<!-- Source: docs/foundation/conformance_suite.md, the conformance matrix. -->

# Reading projection — `failure_posture.md`

Every rule `failure_posture.md` owns, one entry each: the rule's own statement from `conformance_suite.md`'s matrix, and a link to the heading that argues it. The argument, the cost, the prior art, and the walkthrough are in [`failure_posture.md`](../failure_posture.md) and are not repeated here (decision 66).

25 rules.

## [Checkpoints on tasks: one queue, one protocol](../failure_posture.md#checkpoints-on-tasks-one-queue-one-protocol)

- **GW-50** — the same, and : a condition that stops a whole batch is raised on one of its tasks, never on the batch; one per stopped batch; it names the batch and the step *[M (formerly P on X-1, closed)]*
- **FP-16** — reason classes *[M]*
- **FP-17** — the same: escalation reorders, never signs *[M]*
- **FP-18** — the same: `unclaimed_step` after the declared interval, subject one task of the step's batch, naming the batch, the step, the role, and the duration; undeclared raises none *[M]*
- **FP-19** — the same: a role resolving to nobody is caught at declaration *[M]*

## [The decision](../failure_posture.md#the-decision)

- **FP-1** — the rule this heading states *[M]*

## [The rules](../failure_posture.md#the-rules)

- **FP-2** — rule 1: halt work, never stop observing; capture to local disk *[M]*
- **FP-3** — rule 2: announce entering and leaving, aggregated per window *[M (U-19 closed)]*
- **FP-4** — rule 3: the probe is a real read at claim, never the health endpoint *[M]*
- **FP-5** — rule 4: a mid-task write failure leaves the prior state; the lease lapses; no verdict posted elsewhere *[M]*
- **FP-6** — rule 5: deferral is bounded; exhaustion escalates `rounds_exhausted` *[M (`rounds_cap` on the step; U-20 closed)]*
- **FP-7** — rule 6: every write is read back *[M]*
- **FP-8** — rule 7: unknown stays distinct *[M]*
- **FP-9** — rule 8: a failure that left no effect is retried with backoff; a stated retry time is honoured; per system *[M]*

## [The operator-invoked halt, and what undoes an action already taken](../failure_posture.md#the-operator-invoked-halt-and-what-undoes-an-action-already-taken)

- **FP-10** — the halt is confirmed by a read-back of no live leases *[M]*
- **FP-11** — the same: every class names its recovery *[M (U-10 closed)]*
- **FP-12** — the same: a recovery is an action through the gate, under its own class *[M]*
- **FP-13** — the same: the restore obligation *[M (U-11 closed)]*
- **FP-14** — the same: whatever detects does not remediate *[M]*

## [Repeated lapse raises a checkpoint](../failure_posture.md#repeated-lapse-raises-a-checkpoint)

- **FP-15** — the rule this heading states *[M]*

## [What a checkpoint does not absorb](../failure_posture.md#what-a-checkpoint-does-not-absorb)

- **FP-20** — the halt is not a checkpoint *[M]*
- **FP-21** — the same: operator-only tasks are not checkpoints *[M]*

## [Refuse resume-by-replay where actions are consent-gated](../failure_posture.md#refuse-resume-by-replay-where-actions-are-consent-gated)

- **FP-22** — the rule this heading states *[M]*
- **FP-23** — rule 2: the announcement path itself unreachable — the capture is the announcement of last resort, and the path shows no gap on return *[M (U-18 closed)]*
- **FP-24** — the `unclaimed_step` checkpoint's subject *[M (X-1 closed)]*
