<!-- GENERATED by execution/scripts/render_reading_projection.py — do not edit. -->
<!-- Source: docs/foundation/conformance_suite.md, the conformance matrix. -->

# Reading projection — `planning_model.md`

Every rule `planning_model.md` owns, one entry each: the rule's own statement from `conformance_suite.md`'s matrix, and a link to the heading that argues it. The argument, the cost, the prior art, and the walkthrough are in [`planning_model.md`](../planning_model.md) and are not repeated here (decision 66).

12 rules.

## [The hierarchy is edges, and a task has one line upward](../planning_model.md#the-hierarchy-is-edges-and-a-task-has-one-line-upward)

- **PM-1** — one `PART_OF` per task and per record, upward in level, cycle-checked; the ascent derived *[M]*
- **PM-2** — the same: a second membership is `REFERS_TO` or `DEPENDS_ON`, never a second line *[M]*

## [Upward context is a declared read, resolved along the ascent at hydration](../planning_model.md#upward-context-is-a-declared-read-resolved-along-the-ascent-at-hydration)

- **PM-3** — a step declaring a planning type reads it along the ascent at hydration; an ascent ending before the type is an empty read; unplanned is derived and admitted *[M]*
- **PM-4** — the same: `classify` writes the `PART_OF` where the task names a record; `prioritize` may read the ascent *[M for the edge; R for the weighting]*

## [Downward state is derived; upward content is authored, as entities](../planning_model.md#downward-state-is-derived-upward-content-is-authored-as-entities)

- **PM-5** — no stored progress; completion, counts, blockers, next steps derived; decisions as entities; a todo is a task *[M]*
- **PM-6** — the same: a decision reversed is a new `decision` `SUPERSEDES` the old; nothing is merged *[M]*

## [Maintenance is work: the `planning` workflow](../planning_model.md#maintenance-is-work-the-planning-workflow)

- **PM-7** — one live `planning` instance per record; a descendant's close pulls its `due_date` forward; a record with none is unmaintained; the parent's statement is never written by the child's batch *[M]*
- **PM-8** — the same: a lesson at the record's scope is a task `PART_OF` the parent, and the raising batch does not wait *[M]*

## [Authority per level: an amendment is an action, and its class is the level's](../planning_model.md#authority-per-level-an-amendment-is-an-action-and-its-class-is-the-levels)

- **PM-9** — an amendment is an `amend_<level>` action through the gate; a level not listed resolves to `NEVER`; a planning type is not a governance type; an `ownership_grant` on the record seats its principal *[M — the permitted-action half under decision 56's shape: the planning types' authored fields on the engine's grant alone, a `planner`'s write to a statement refused at admission; and the operator's own amendment at the reserved level held, self-resolved and marked, and written by the engine (decision 58)]*

## [Binding dissolves: a task's ascent is its binding](../planning_model.md#binding-dissolves-a-tasks-ascent-is-its-binding)

- **PM-10** — a session binds nothing and corrects no planning field; its output is tasks under records *[M for admission (by grant, ruling 41); D for the session's own conduct, as U-12]*

## [The mechanism against cross-record collision is the subject](../planning_model.md#the-mechanism-against-cross-record-collision-is-the-subject)

- **PM-11** — a planning write lands only on the record the batch's task is `PART_OF` *[M — the engine checks the producing task's ascent before it writes, and no other grant admits the type (decision 56's shape)]*

## [Which levels an instance declares, and what it calls them](../planning_model.md#which-levels-an-instance-declares-and-what-it-calls-them)

- **PM-12** — the rule this heading states *[P (open decision 57; the design reads the mark and never a level by name, so no row goes red under any answer); decision 58 is ruled and tested under PM-9's operator case]*
