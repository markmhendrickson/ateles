<!-- GENERATED by execution/scripts/render_reading_projection.py — do not edit. -->
<!-- Source: docs/foundation/conformance_suite.md, the conformance matrix. -->

# Reading projection — `payments.md`

Every rule `payments.md` owns, one entry each: the rule's own statement from `conformance_suite.md`'s matrix, and a link to the heading that argues it. The argument, the cost, the prior art, and the walkthrough are in [`payments.md`](../payments.md) and are not repeated here (decision 66).

17 rules.

## [The dedup key, and what it is keyed on](../payments.md#the-dedup-key-and-what-it-is-keyed-on)

- **PY-1** — keyed on the obligation, written before the first attempt; a confirmed key refused *[M]*

## [The unknown case: a transfer submitted whose confirmation never returned](../payments.md#the-unknown-case-a-transfer-submitted-whose-confirmation-never-returned)

- **PY-2** — and its five parts ([`payments.md#the-two-questions-which-must-not-be-conflated`](../payments.md#the-two-questions-which-must-not-be-conflated), [`payments.md#what-the-record-holds-while-it-is-unknown`](../payments.md#what-the-record-holds-while-it-is-unknown), [`payments.md#how-the-read-resolves-it-per-rail-class`](../payments.md#how-the-read-resolves-it-per-rail-class), [`payments.md#when-the-read-cannot-resolve-it`](../payments.md#when-the-read-cannot-resolve-it), [`payments.md#the-complete-rule-stated-once`](../payments.md#the-complete-rule-stated-once)): a timeout is `unknown`; no artifact; `reconcile` stays open; the read precedes any resubmission; crypto rebroadcasts the identical bytes; bank adopts what the window finds; exhaustion raises one checkpoint with what was read and three options; the adapter never chooses *[M]*

## [Terminal is not permanent, and the design must not assume it is](../payments.md#terminal-is-not-permanent-and-the-design-must-not-assume-it-is)

- **PY-3** — the terminal condition is declared; a reversal is an observation and a defect, never a silent correction or a resubmission *[M]*

## [Terminal is declared in the rail's adapter document, and the value is bound per instance](../payments.md#terminal-is-declared-in-the-rails-adapter-document-and-the-value-is-bound-per-instance)

- **PY-3a** — settled, never sent, on a bank rail; *N* confirmations on a chain; the value in the `vendor_binding`; a profile may deepen and never shallow *[M]*

## [Reading a balance: an observation, and not an artifact](../payments.md#reading-a-balance-an-observation-and-not-an-artifact)

- **PY-4** — on the account's artifact, with point and confirmed-versus-pending; never a permit *[M]*

## [Fees, rates, and what the operator consented to](../payments.md#fees-rates-and-what-the-operator-consented-to)

- **PY-5** — the adapter never widens what was approved; a fee is disclosed *[M]*

## [Tolerance is an `action_policy` value, and its default is zero](../payments.md#tolerance-is-an-action_policy-value-and-its-default-is-zero)

- **PY-5a** — absent reads as zero; a change outside it is an observation, `consent`'s `on_fail` opens `verify` again, and the operator decides on the new figures; a profile may tighten and never widen *[M]*

## [The reference field, and a policy that suppresses it](../payments.md#the-reference-field-and-a-policy-that-suppresses-it)

- **PY-6** — omission, never a placeholder; a property of what is submitted; verified on read-back; the record narrowed too *[M]*

## [Events about a transfer the swarm submitted](../payments.md#events-about-a-transfer-the-swarm-submitted)

- **PY-7** — a non-terminal state is not a confirmation; a notification is never a confirmation on its own; a transfer with no action is a defect *[M]*

## [Outbound: the operations a step takes on a rail](../payments.md#outbound-the-operations-a-step-takes-on-a-rail)

- **PY-8** — `payment` is in the never-set and never graduates; the adapter never writes `reconcile` *[M]*

## [What the adapter refuses, and why](../payments.md#what-the-adapter-refuses-and-why)

- **PY-9** — the rule this heading states *[M]*

## [A payment's approver is shown exactly what the verifier signed](../payments.md#a-payments-approver-is-shown-exactly-what-the-verifier-signed)

- **PY-10** — the rule this heading states *[M]*

## [What the rails hold, and what an artifact is here](../payments.md#what-the-rails-hold-and-what-an-artifact-is-here)

- **PY-11** — a bank transfer's id exists only when the response returns and a chain transaction's before submission; a receipt is an artifact; the `transaction` entity is not *[M]*

## [What gates a payment, and why it is not a new mechanism](../payments.md#what-gates-a-payment-and-why-it-is-not-a-new-mechanism)

- **PY-12** — four existing mechanisms compose; no second gate; `payment` never graduates *[M]*

## [Events about money the swarm did not send](../payments.md#events-about-money-the-swarm-did-not-send)

- **PY-13** — [`payments.md#delivery-and-what-the-dedup-rule-keys-on-inbound`](../payments.md#delivery-and-what-the-dedup-rule-keys-on-inbound): an incoming payment is matched to an obligation by a step's signed judgement, never by the adapter reading the reference text; an outgoing payment with no action is a defect; the inbound key is the per-delivery id on a bank rail and the transaction with its height on a chain; a notification is never a confirmation *[M]*

## [Recovery: what undoes a payment](../payments.md#recovery-what-undoes-a-payment)

- **PY-14** — [`payments.md#what-the-rails-offer-that-this-design-does-not-use`](../payments.md#what-the-rails-offer-that-this-design-does-not-use): cancel is a reversal only before funding; a recall is a request that never returns success; a chain confirmation has no recovery; a replacement is a new action through the gate; scheduled, bulk, and rail-side approval features are unused *[M]*

## [How the five rules apply here](../payments.md#how-the-five-rules-apply-here)

- **PY-15** — the payee is resolved by reference to the profile, never by matching a name, an address, or a reference string; a rail's indeterminate state is `unknown`, never pending or failed *[M]*
