# Language Command

## Purpose

Direct users and agents to the canonical language workflow (fluency, translation) implemented in the language skill.

## Scope

Pointer document only; applies when invoking `/language` or language-related prompts. Full workflow lives in the skill.

---

Canonical workflow now lives in the `language` skill:
`/.cursor/skills/language/SKILL.md`

Invoke with `/language` (or equivalent natural-language trigger) to load and execute the skill workflow.
