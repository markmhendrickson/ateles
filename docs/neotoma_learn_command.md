# Neotoma Learn Command

## Purpose

Direct users and agents to the canonical Neotoma-learn workflow (update MCP instructions from failures) in the neotoma-learn skill.

## Scope

Pointer document only; applies when invoking `/neotoma_learn` or when improving Neotoma MCP instructions. Full workflow lives in the skill.

---

Canonical workflow now lives in the `neotoma-learn` skill:
`/.cursor/skills/neotoma-learn/SKILL.md`

Invoke with `/neotoma_learn` (or equivalent natural-language trigger) to load and execute the skill workflow.
