# ⚠️ DEPRECATED: Execution Layer MCP Servers

**This directory is deprecated. All MCP servers have been consolidated into `mcp/`.**

**Please use:** `mcp/` for all MCP servers (data access and external API integrations).

See `mcp/README.md` for current server locations and configuration.

---

# Execution Layer MCP Servers (DEPRECATED)

This directory **was** used for MCP servers for external API integrations and action execution. These servers have been moved to `mcp/`.

## Migration

All servers have been moved to `mcp/`:
- `gmail/` → `mcp/gmail/`
- `dnsimple/` → `mcp/dnsimple/`
- `google-calendar/` → `mcp/google-calendar/`
- `instagram/` → `mcp/instagram/`
- `minted/` → `mcp/minted/`
- `asana/` → `mcp/asana/`
- `whatsapp/` → `mcp/whatsapp/`
- `homekit/` → `mcp/homekit/` (note: some router setup docs remain here temporarily)

## Current Configuration

See `mcp/README.md` and `mcp/mcp-config-template.json` for current server configuration.
