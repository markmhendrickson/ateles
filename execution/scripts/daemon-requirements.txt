# Runtime dependencies for the Ateles T3 daemons (Apis, Formica, neotoma-agent).
# Pinned so the RC deploy checkout (~/ateles-rc-src/.venv) provisions
# deterministically via redeploy_daemons_from_main.sh. Keep in sync with the
# dev checkout's venv when daemon imports change.
httpx==0.28.1
httpx-sse==0.4.3
aiohttp==3.13.5
cryptography==48.0.0
PyJWT==2.13.0
